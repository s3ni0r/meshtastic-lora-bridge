diff --git a/src/configuration.h b/src/configuration.h
index 75cdac5..8446cc4 100644
--- a/src/configuration.h
+++ b/src/configuration.h
@@ -292,6 +292,24 @@ along with this program.  If not, see <http://www.gnu.org/licenses/>.
 #define GPS_BAUDRATE_FIXED 1
 #endif
 
+// Project fork: -DGPS_TAG = self-contained GPS tag flavor (onboard AG3335 -> PRIVATE_APP(256) LoRa
+// stream, same payload as the ODID bridge). One flag pulls in the whole role so a build can't end up
+// with the GPS fast-path but no sender (or vice versa). The ODID bridge keeps its explicit flag set.
+#ifdef GPS_TAG
+#ifdef ODID_SNIFFER
+#error "GPS_TAG and ODID_SNIFFER are mutually exclusive tag flavors - pick one position source"
+#endif
+#ifndef HIGHRATE_POSITION_SENDER
+#define HIGHRATE_POSITION_SENDER 1
+#endif
+#ifndef HIGHRATE_TX_ONLY
+#define HIGHRATE_TX_ONLY 1 // pure sender: LR1110 idles in standby, never RX (see LR11x0Interface.cpp)
+#endif
+#ifndef GPS_THREAD_INTERVAL
+#define GPS_THREAD_INTERVAL 100 // 4 Hz+ fixes shouldn't wait on the stock 200 ms parser tick
+#endif
+#endif
+
 #ifndef GPS_THREAD_INTERVAL
 #define GPS_THREAD_INTERVAL 200
 #endif
diff --git a/src/gps/GPS.cpp b/src/gps/GPS.cpp
index 6c6cdba..f0b6778 100644
--- a/src/gps/GPS.cpp
+++ b/src/gps/GPS.cpp
@@ -32,6 +32,27 @@
 #define GPS_RESET_MODE HIGH
 #endif
 
+#ifdef GPS_TAG
+// Project fork: self-contained GPS tag. Every published AG3335 fix is stashed here for the LoRa
+// sender (HighRatePositionModule reads these — the GPS analog of the sniffer's g_odid* globals) and
+// the sender is woken immediately, so a fresh fix transmits in ~ms instead of aging up to a poll
+// tick. GnssRateProbe (ticked from runOnce) tries to raise the fix rate to 4 Hz+ on this unit.
+#include "GnssRateProbe.h"
+#include "modules/HighRatePositionModule.h"
+volatile int32_t g_gpsTagLat = 0;     // deg*1e7
+volatile int32_t g_gpsTagLon = 0;
+volatile int16_t g_gpsTagAlt = 0;     // MSL altitude, m
+volatile uint8_t g_gpsTagSpeed = 0;   // ground speed, km/h (capped 255)
+volatile uint8_t g_gpsTagHeading = 0; // heading, deg * 256/360
+volatile uint8_t g_gpsTagHacc = 0;    // ~horizontal accuracy, m (HDOP x 3 m UERE; 0 = unknown)
+volatile uint32_t g_gpsTagMs = 0;     // millis() of the last stash (0 = none yet)
+volatile uint16_t g_gpsTagTs = 0;     // fix time-of-hour in 0.1 s (incl. centiseconds) — novelty key
+// GST-derived horizontal accuracy, parsed by GnssRateProbe's raw tap (TinyGPS++ custom fields
+// are compiled out on this platform): metres, 0 = none seen; g_gstMs = millis() of last update.
+extern volatile uint8_t g_gstHaccM;
+extern volatile uint32_t g_gstMs;
+#endif
+
 // Not all platforms have std::size().
 template <typename T, std::size_t N> std::size_t array_count(const T (&)[N])
 {
@@ -652,6 +673,10 @@ bool GPS::setup()
 
             delay(250);
             _serial_gps->write("$PAIR513*3D\r\n"); // save configuration
+            // NOTE: this AG3335 firmware LOCKS the fix rate at 1 Hz. Verified on-device: $PAIR050
+            // (Airoha) and $PMTK220/$PMTK300 (MediaTek) rate commands get no ACK and no effect (RAM,
+            // save, and save+RESETB all stay 1 Hz), while $PAIR062 sentence config works. The only
+            // documented unlock ($PAIR382,1 engine-stop) bricks it into backup sleep. See FORK.md §3.
         } else if (gnssModel == GNSS_MODEL_UBLOX6) {
             clearBuffer();
             SEND_UBX_PACKET(0x06, 0x02, _message_DISABLE_TXT_INFO, "disable text info messages", 500);
@@ -1070,6 +1095,36 @@ void GPS::publishUpdate()
         if (config.position.gps_mode == meshtastic_Config_PositionConfig_GpsMode_ENABLED) {
             positionModule->handleNewPosition();
         }
+
+#ifdef GPS_TAG
+        // Project fork: stash the fix for the PRIVATE_APP(256) sender and wake it NOW (event-driven
+        // send, same cross-task pattern as the ODID sniffer). Only real fixes — the loss-of-lock
+        // publish (p reset to defaults) must not overwrite the last good stash; the sender's own
+        // freshness window handles going stale.
+        if (hasValidLocation && (p.latitude_i != 0 || p.longitude_i != 0)) {
+            g_gpsTagLat = p.latitude_i;
+            g_gpsTagLon = p.longitude_i;
+            int32_t altM = p.altitude;
+            g_gpsTagAlt = (int16_t)(altM < -32768 ? -32768 : (altM > 32767 ? 32767 : altM));
+            uint32_t spdKmh = p.ground_speed; // stored in km/h by lookForLocation (reader.speed.kmph())
+            g_gpsTagSpeed = (uint8_t)(spdKmh > 255 ? 255 : spdKmh);
+            g_gpsTagHeading = (uint8_t)((((p.ground_track / 100000) % 360) * 256) / 360); // deg*1e5 -> deg
+            // Preferred: the receiver's own GST error estimate (probe tap); fallback: HDOP heuristic.
+            uint32_t haccM = 0;
+            if (g_gstHaccM != 0 && (millis() - g_gstMs) < 1500)
+                haccM = g_gstHaccM;
+            if (haccM == 0 && p.HDOP != 0)
+                haccM = ((uint32_t)p.HDOP * 3) / 100; // HDOP (x10^2) x ~3 m UERE
+            g_gpsTagHacc = (uint8_t)(haccM == 0 ? 0 : (haccM > 255 ? 255 : (haccM < 1 ? 1 : haccM)));
+            // 0.1 s-in-hour timestamp incl. centiseconds: novelty key that ticks even when parked
+            // (coords alone can freeze between fixes) — mirrors the ODID Location ts semantics.
+            g_gpsTagTs = (uint16_t)((((uint32_t)reader.time.minute() * 60 + reader.time.second()) * 10) +
+                                    (reader.time.centisecond() / 10));
+            g_gpsTagMs = millis();
+            if (highRatePositionModule)
+                highRatePositionModule->wakeFreshFix();
+        }
+#endif
     }
 }
 
@@ -1119,6 +1174,13 @@ int32_t GPS::runOnce()
         setConnected();
     }
 
+#ifdef GPS_TAG
+    // Project fork: drive the GNSS rate-raise probe + live fix-rate monitor (non-blocking; walks
+    // safe RAM-only rate candidates once at boot, then logs the measured cadence every 10 s).
+    if (powerState == GPS_ACTIVE)
+        gnssRateProbe.tick(_serial_gps, reader);
+#endif
+
     // If we're due for an update, wake the GPS
     if (!config.position.fixed_position && powerState != GPS_ACTIVE && scheduling.isUpdateDue())
         up();
@@ -1313,6 +1375,26 @@ GnssModel_t GPS::probe(int serialSpeed)
     }
     case 3: {
         /* Airoha (Mediatek) AG3335A/M/S, A3352Q, Quectel L89 2.0, SimCom SIM65M */
+#ifdef HIGHRATE_POSITION_SENDER
+        // Keep the module awake + out of backup sleep before probing (anti-brick; see FORK.md).
+        _serial_gps->write("$PAIR002*38\r\n"); // GNSS power ON
+        delay(200);
+#ifdef GPS_TAG
+        // $PAIR382,1 = LOCK system sleep (KEEP AWAKE) — per the LC29H spec and Seeed's own
+        // ag3335.c, which blasts it 25x at every scan start. This firmware's command CPU
+        // auto-sleeps a few seconds after boot (NMEA keeps streaming, but incoming commands are
+        // silently ignored — why $PAIR050 "never ACKed" historically). Latching the lock inside
+        // the boot window keeps the command interface alive for GnssRateProbe. Send it several
+        // times like Seeed does: only one needs to land while the CPU is awake.
+        for (uint8_t i = 0; i < 6; i++) {
+            _serial_gps->write("$PAIR382,1*2E\r\n");
+            delay(60);
+        }
+#else
+        _serial_gps->write("$PAIR382,0*2F\r\n"); // legacy preamble (bridge builds never power GPS)
+        delay(200);
+#endif
+#endif
         _serial_gps->write("$PAIR062,2,0*3C\r\n"); // GSA OFF to reduce volume
         _serial_gps->write("$PAIR062,3,0*3D\r\n"); // GSV OFF to reduce volume
         _serial_gps->write("$PAIR513*3D\r\n");     // save configuration
@@ -1570,6 +1652,25 @@ GPS *GPS::createGps()
     // Make sure the GPS is awake before performing any init.
     new_gps->up();
 
+#if defined(HIGHRATE_POSITION_SENDER) && defined(GPS_RTC_INT)
+    // GNSS hard-recovery (project fork): an earlier build sent $PAIR382,1 + $PAIR003 and left the
+    // AG3335 in a VRTC-backed backup sleep that survives reboots and ignores UART (so $PAIR021 probes
+    // get no reply). The wake is the GPS_RTC_INT line going HIGH (variant: "normal LOW, wake by HIGH").
+    // Pulse it awake, hold a longer hardware reset, then return to normal before probing.
+    pinMode(GPS_RTC_INT, OUTPUT);
+    digitalWrite(GPS_RTC_INT, HIGH); // wake from backup sleep
+    delay(300);
+#ifdef PIN_GPS_RESET
+    pinMode(PIN_GPS_RESET, OUTPUT);
+    digitalWrite(PIN_GPS_RESET, GPS_RESET_MODE);
+    delay(60);
+    digitalWrite(PIN_GPS_RESET, !GPS_RESET_MODE);
+#endif
+    delay(400);
+    digitalWrite(GPS_RTC_INT, LOW); // back to normal level
+    delay(200);
+#endif
+
 #ifdef PIN_GPS_RESET
     pinMode(PIN_GPS_RESET, OUTPUT);
     digitalWrite(PIN_GPS_RESET, GPS_RESET_MODE); // assert for 10ms
@@ -1822,6 +1923,9 @@ bool GPS::whileActive()
     // First consume any chars that have piled up at the receiver
     while (_serial_gps->available() > 0) {
         int c = _serial_gps->read();
+#ifdef GPS_TAG
+        gnssRateProbe.feedByte(c); // raw tap: captures $PAIR001 ACKs + $PAIR021/$PAIR051 replies
+#endif
         UBXscratch[charsInBuf] = c;
 #ifdef GPS_DEBUG
         debugmsg += vformat("%c", (c >= 32 && c <= 126) ? c : '.');
diff --git a/src/main.cpp b/src/main.cpp
index c654822..4c59a64 100644
--- a/src/main.cpp
+++ b/src/main.cpp
@@ -913,6 +913,12 @@ void setup()
 #ifdef SENSOR_GPS_CONFLICT
     if (sensor_detected == false) {
 #endif
+#ifdef ODID_SNIFFER
+        // ODID sniffer bridge: the onboard AG3335 is firmware-locked at 1 Hz and unused — never
+        // create/power/probe it. Position comes from the sniffed Dronetag Remote ID. Skipping
+        // createGps() means no `new GPS`, no Serial1, no up()/PIN_GPS_EN — the GNSS stays unpowered.
+        LOG_INFO("ODID_SNIFFER build: onboard GPS disabled (Dronetag RID is the position source)");
+#else
         if (HAS_GPS) {
             if (config.position.gps_mode != meshtastic_Config_PositionConfig_GpsMode_NOT_PRESENT) {
                 gps = GPS::createGps();
@@ -923,6 +929,7 @@ void setup()
                 }
             }
         }
+#endif
 #ifdef SENSOR_GPS_CONFLICT
     }
 #endif
diff --git a/src/mesh/LR11x0Interface.cpp b/src/mesh/LR11x0Interface.cpp
index 3831a38..881c692 100644
--- a/src/mesh/LR11x0Interface.cpp
+++ b/src/mesh/LR11x0Interface.cpp
@@ -233,7 +233,13 @@ template <typename T> void LR11x0Interface<T>::configHardwareForSend()
 
 template <typename T> void LR11x0Interface<T>::startReceive()
 {
-#ifdef SLEEP_ONLY
+#if defined(HIGHRATE_TX_ONLY)
+    // Fork: TX-only bridge node — the ODID Tag only *relays*; it never needs to hear the mesh. Idle in
+    // STANDBY instead of RX so an in-progress foreign RX can never defer our TX (LoRa is half-duplex),
+    // and standby draws uA vs mA in continuous RX. CAD (isChannelActive) still runs before TX, and the
+    // TX-done IRQ is wired in startSend(), so the transmit path is unaffected.
+    setStandby();
+#elif defined(SLEEP_ONLY)
     sleep();
 #else
 
diff --git a/src/modules/Modules.cpp b/src/modules/Modules.cpp
index e477574..ff43800 100644
--- a/src/modules/Modules.cpp
+++ b/src/modules/Modules.cpp
@@ -107,6 +107,13 @@
 #include "modules/DropzoneModule.h"
 #endif
 
+#ifdef HIGHRATE_POSITION_SENDER
+#include "modules/HighRatePositionModule.h"
+#endif
+#ifdef GPS_TAG
+#include "modules/GnssConfigModule.h"
+#endif
+
 /**
  * Create module instances here.  If you are adding a new module, you must 'new' it here (or somewhere else)
  */
@@ -128,6 +135,12 @@ void setupModules()
 #if !MESHTASTIC_EXCLUDE_GPS
     positionModule = new PositionModule();
 #endif
+#ifdef HIGHRATE_POSITION_SENDER
+    highRatePositionModule = new HighRatePositionModule();
+#endif
+#ifdef GPS_TAG
+    gnssConfigModule = new GnssConfigModule();
+#endif
 #if !MESHTASTIC_EXCLUDE_WAYPOINT
     waypointModule = new WaypointModule();
 #endif
diff --git a/src/modules/Telemetry/DeviceTelemetry.h b/src/modules/Telemetry/DeviceTelemetry.h
index a1d55a5..fe8114c 100644
--- a/src/modules/Telemetry/DeviceTelemetry.h
+++ b/src/modules/Telemetry/DeviceTelemetry.h
@@ -45,7 +45,11 @@ class DeviceTelemetryModule : private concurrency::OSThread, public ProtobufModu
     meshtastic_Telemetry getLocalStatsTelemetry();
 
     void sendLocalStatsToPhone();
-    uint32_t sendToPhoneIntervalMs = SECONDS_IN_MINUTE * 1000;           // Send to phone every minute
+    // Fork: 15 s instead of stock 60 s — the MeshTracker app shows the connected node's battery,
+    // and the Base has no other channel for it (it never emits the PRIVATE_APP position stream).
+    // BLE-only push, so faster costs no LoRa airtime; 15 s also caps the blank-gauge window
+    // right after the phone connects.
+    uint32_t sendToPhoneIntervalMs = 15 * 1000;
     uint32_t sendStatsToPhoneIntervalMs = 15 * SECONDS_IN_MINUTE * 1000; // Send stats to phone every 15 minutes
     uint32_t lastSentStatsToPhone = 0;
     uint32_t lastSentToMesh = 0;
diff --git a/src/platform/nrf52/NRF52Bluetooth.cpp b/src/platform/nrf52/NRF52Bluetooth.cpp
index 4f7fb47..c22c41f 100644
--- a/src/platform/nrf52/NRF52Bluetooth.cpp
+++ b/src/platform/nrf52/NRF52Bluetooth.cpp
@@ -240,13 +240,169 @@ int NRF52Bluetooth::getRssi()
 {
     return 0; // FIXME figure out where to source this
 }
+#ifdef ODID_SNIFFER
+// --- Open Drone ID (ASTM F3411 Remote ID) advertisement sniffer ----------------------------------
+// A co-located Dronetag broadcasts legacy 1M adverts: Service Data (AD 0x16), UUID 0xFFFA, app code
+// 0x0D, a 1-byte rolling counter, then a 25-byte ODID message (or a 0xF "message pack"). We decode
+// the Location message (type 0x1) — lat/lon are int32 LE deg*1e7 (same as our LoRa payload) — and the
+// 0.1s TimeStamp, logging inter-arrival dt so we can measure the TRUE position-novelty rate.
+// Latest sniffed Dronetag fix, handed off to the LoRa sender (HighRatePositionModule reads these).
+volatile int32_t g_odidLat = 0;     // deg*1e7
+volatile int32_t g_odidLon = 0;
+volatile int16_t g_odidAlt = 0;     // geo altitude, m
+volatile uint8_t g_odidSpeed = 0;   // ground speed, km/h (capped 255)
+volatile uint8_t g_odidHeading = 0; // heading, deg * 256/360
+volatile uint8_t g_odidHacc = 0;    // horizontal accuracy, m (0 = unknown / coarse > 185 m)
+volatile uint32_t g_odidMs = 0;     // millis() of the last valid decode (0 = none yet)
+volatile uint16_t g_odidTs = 0;     // ODID fix timestamp (0.1 s since top-of-hour) — novelty detection
+
+#if defined(HIGHRATE_POSITION_SENDER)
+#include "modules/HighRatePositionModule.h" // wakeFreshFix(): event-driven send on novel fixes
+#endif
+
+// ODID horizontal-accuracy enum (low nibble of byte 19) -> meters upper bound (0 = unknown / coarse).
+// Per ASTM F3411 / opendroneid: 8=<92.6m, 9=<30m, 10=<10m, 11=<3m, 12=<1m; 0-7 + 13-15 coarse/unknown.
+static const uint8_t odidHaccMeters[16] = {0, 0, 0, 0, 0, 0, 0, 0, 93, 30, 10, 3, 1, 0, 0, 0};
+
+static uint32_t odidLastMs = 0;
+// Benchmark instrumentation: scan-callback load + extended/Location split, logged every 5 s.
+static uint32_t sCb = 0, sExt = 0, sLoc = 0, sStatsMs = 0;
+
+static void odidDecodeLocation(const uint8_t *m, uint8_t counter, int8_t rssi)
+{
+    sLoc++;
+    int32_t lat = (int32_t)((uint32_t)m[5] | (uint32_t)m[6] << 8 | (uint32_t)m[7] << 16 | (uint32_t)m[8] << 24);
+    int32_t lon = (int32_t)((uint32_t)m[9] | (uint32_t)m[10] << 8 | (uint32_t)m[11] << 16 | (uint32_t)m[12] << 24);
+    // Vector fields. byte1 = [SpeedMult:1][EWDir:1][HeightType:1][Rsvd:1][Status:4].
+    bool speedMult = m[1] & 0x01;
+    bool ewDir = (m[1] >> 1) & 0x01;
+    int hdgDeg = (m[2] <= 179) ? (m[2] + (ewDir ? 180 : 0)) : 0;                         // 0..359
+    float spdMps = (m[3] == 0xFF) ? 0.0f : (speedMult ? (m[3] * 0.75f + 63.75f) : (m[3] * 0.25f));
+    int spdKmh = (int)(spdMps * 3.6f + 0.5f);
+    int16_t altM = (int16_t)((int)((uint16_t)(m[15] | (m[16] << 8))) / 2 - 1000);        // 0.5 m/LSB, -1000
+    uint8_t hAcc = odidHaccMeters[m[19] & 0x0F];
+    uint16_t ts = (uint16_t)(m[21] | (m[22] << 8)); // ODID fix timestamp, tenths of sec since top-of-hour
+
+    uint32_t now = millis();
+    if (lat != 0 || lon != 0) { // valid fix -> hand off to the LoRa sender
+        // NOVEL = the Dronetag produced a new fix (vs a re-advert of the same one, ~5 Hz). Only novelty
+        // is worth a LoRa send, a log line, or a main-loop wake.
+        bool novel = (ts != g_odidTs) || (lat != g_odidLat) || (lon != g_odidLon);
+        g_odidLat = lat;
+        g_odidLon = lon;
+        g_odidAlt = altM;
+        g_odidSpeed = (uint8_t)(spdKmh > 255 ? 255 : spdKmh);
+        g_odidHeading = (uint8_t)((hdgDeg * 256) / 360);
+        g_odidHacc = hAcc;
+        g_odidTs = ts;
+        g_odidMs = now;
+        if (novel) {
+            uint32_t dt = odidLastMs ? (now - odidLastMs) : 0; // novelty inter-arrival (true fix rate)
+            odidLastMs = now;
+            LOG_INFO("ODID LOC cnt=%u dt=%lums ts=%u lat=%d lon=%d alt=%dm spd=%dkmh hdg=%d hacc=%um rssi=%d",
+                     counter, (unsigned long)dt, ts, lat, lon, altM, spdKmh, hdgDeg, hAcc, rssi);
+#if defined(HIGHRATE_POSITION_SENDER)
+            // Event-driven send: schedule the sender NOW instead of letting the fix age up to a full
+            // poll interval (was 0-250 ms, mean ~125 ms — the dominant Tag-side latency term).
+            if (highRatePositionModule)
+                highRatePositionModule->wakeFreshFix();
+#endif
+        }
+    }
+}
+
+static void odidScanCb(ble_gap_evt_adv_report_t *report)
+{
+    sCb++;
+    if (report->type.extended_pdu)
+        sExt++;
+#if defined(ODID_PHY_EXT)
+    // BT5 mode: decode ONLY extended packs — skip every legacy PDU (the Dronetag's own 4-5 legacy
+    // adverts AND nearby BLE4 noise) so the data is one clean pack per cycle.
+    if (!report->type.extended_pdu) {
+        Bluefruit.Scanner.resume();
+        return;
+    }
+#endif
+    uint8_t buf[255]; // a BT5 extended ODID message pack can be ~250 B; BT4 legacy uses <= 31
+    int8_t rssi = report->rssi;
+    uint8_t len = Bluefruit.Scanner.parseReportByType(report, BLE_GAP_AD_TYPE_SERVICE_DATA, buf, sizeof(buf));
+    // Resume IMMEDIATELY after copying the report out: S140 pauses scanning from report delivery until
+    // resume(), so every ms of decode/log work done first = adverts silently missed. Everything below
+    // works on our copy (`buf`) — `report` must not be touched past this point.
+    Bluefruit.Scanner.resume();
+
+    uint32_t nowS = millis();
+    if (sStatsMs == 0)
+        sStatsMs = nowS;
+    if (nowS - sStatsMs >= 5000) {
+        LOG_INFO("SNIFF cb=%u ext=%u loc=%u in %lums", sCb, sExt, sLoc, (unsigned long)(nowS - sStatsMs));
+        sCb = sExt = sLoc = 0;
+        sStatsMs = nowS;
+    }
+    // Expect: [0xFA][0xFF] (UUID 0xFFFA) [0x0D] (app) [counter] [ODID message(s)]
+    if (len >= 5 && buf[0] == 0xFA && buf[1] == 0xFF && buf[2] == 0x0D) {
+        uint8_t counter = buf[3];
+        const uint8_t *odid = &buf[4];
+        uint8_t odidLen = len - 4;
+        uint8_t type = (odid[0] >> 4) & 0x0F;
+        if (type == 0x1 && odidLen >= 23) { // single Location message (BT4 legacy)
+            odidDecodeLocation(odid, counter, rssi);
+        } else if (type == 0xF && odidLen >= 3 && odid[1] == 25) { // BT5 message pack of 25-byte msgs
+            uint8_t qty = odid[2];
+            if (qty > 9) // ODID_PACK_MAX_MESSAGES
+                qty = 9;
+            for (uint8_t i = 0; i < qty; i++) {
+                const uint8_t *m = odid + 3 + i * 25;
+                if ((uint32_t)((m + 25) - odid) > odidLen) // require a full 25-byte message in-bounds
+                    break;
+                if (((m[0] >> 4) & 0x0F) == 0x1) {
+                    odidDecodeLocation(m, counter, rssi);
+                    break;
+                }
+            }
+        }
+    }
+    // (Scanner already resumed above, right after the report copy-out.)
+}
+
+static void startOdidScanner(void)
+{
+    Bluefruit.Scanner.setRxCallback(odidScanCb);
+    Bluefruit.Scanner.useActiveScan(false);     // ODID is in the primary/extended advert, not scan-rsp
+    Bluefruit.Scanner.setInterval(160, 160);    // CONTINUOUS scan (window==interval) — catch every advert
+    Bluefruit.Scanner.restartOnDisconnect(true);
+    // PHY/extended selection on the live scan-params pointer (Bluefruit exposes no setter for these).
+    ble_gap_scan_params_t *sp = Bluefruit.Scanner.getParams();
+#if defined(ODID_PHY_EXT)
+    // BT5: accept EXTENDED advertising on the 1M secondary channel — the Dronetag's BT5 path packs all
+    // ODID messages into one PDU. REQUIRES the 255-byte scan buffer (firmware/patch-bluefruit-ext.sh).
+    sp->extended = 1;
+    // Dronetag "BT5 Long Range" = CODED PHY (not 1M extended). Scanning Coded also rejects ALL 1M
+    // legacy outright (the Dronetag's own + nearby BLE4 noise) — only the Coded message-pack is seen.
+    sp->scan_phys = BLE_GAP_PHY_CODED;
+    sp->report_incomplete_evts = 0;
+    LOG_INFO("ODID sniffer: BT5 EXTENDED scan on CODED PHY / Long Range (255B buffer)");
+#else
+    sp->extended = 0;                           // BT4 legacy 1M (default; best Location refresh)
+    sp->scan_phys = BLE_GAP_PHY_AUTO;
+    LOG_INFO("ODID sniffer: BT4 legacy continuous scan (scan-only, GPS off)");
+#endif
+    Bluefruit.Scanner.start(0);                 // 0 = scan forever
+}
+#endif // ODID_SNIFFER
+
 void NRF52Bluetooth::setup()
 {
     // Initialise the Bluefruit module
     LOG_INFO("Init the Bluefruit nRF52 module");
     Bluefruit.autoConnLed(false);
     Bluefruit.configPrphBandwidth(BANDWIDTH_MAX);
+#ifdef ODID_SNIFFER
+    Bluefruit.begin(1, 1); // 1 peripheral (phone) + 1 observer (Remote ID sniffer)
+#else
     Bluefruit.begin();
+#endif
     // Clear existing data.
     Bluefruit.Advertising.stop();
     Bluefruit.Advertising.clearData();
@@ -292,10 +448,19 @@ void NRF52Bluetooth::setup()
     // BLEService and BLECharacteristic classes
     LOG_INFO("Init the Mesh bluetooth service");
     setupMeshService();
+#ifdef ODID_SNIFFER
+    // Scan-only bridge: do NOT advertise. With no advertising and no phone connection the SoftDevice
+    // gives the BLE observer ~100% radio time. Configure the Tag over USB. (Keeping begin(1,1) and just
+    // skipping startAdv is safer than begin(0,1), which skips per-peripheral CONN_CFG setup.)
+    Bluefruit.Advertising.stop();
+    LOG_INFO("ODID_SNIFFER: scan-only, advertising disabled");
+    startOdidScanner();
+#else
     // Setup the advertising packet(s)
     LOG_INFO("Set up the advertising payload(s)");
     startAdv();
     LOG_INFO("Advertise");
+#endif
 }
 void NRF52Bluetooth::resumeAdvertising()
 {
diff --git a/variants/nrf52840/tracker-t1000-e/platformio.ini b/variants/nrf52840/tracker-t1000-e/platformio.ini
index 905d751..b719605 100644
--- a/variants/nrf52840/tracker-t1000-e/platformio.ini
+++ b/variants/nrf52840/tracker-t1000-e/platformio.ini
@@ -13,6 +13,11 @@ build_flags = ${nrf52840_base.build_flags}
   -DMESHTASTIC_EXCLUDE_DETECTIONSENSOR=1
   -DMESHTASTIC_EXCLUDE_WIFI=1
 board_build.ldscript = src/platform/nrf52/nrf52840_s140_v7.ld
+; Keep the base hook (UF2 gen etc.) AND auto-apply the Bluefruit 255B scan-buffer patch for the BT5
+; (-DODID_PHY_EXT) ODID sniffer, so extended builds need no manual patch step.
+extra_scripts =
+  bin/platformio-custom.py
+  pre:patch_bluefruit_ext.py
 build_src_filter = ${nrf52_base.build_src_filter} +<../variants/nrf52840/tracker-t1000-e>
 lib_deps = 
   ${nrf52840_base.lib_deps}
